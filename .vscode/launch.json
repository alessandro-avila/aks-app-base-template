{
    "configurations": [
        {
            "name": "Docker Node.js Launch",
            "type": "docker",
            "request": "launch",
            "preLaunchTask": "docker-run: debug",
            "platform": "node",
            "node": {
                "package": "${workspaceFolder}/src/app/src/package.json",
                "localRoot": "${workspaceFolder}/src/app/src"
            }
        }
    ]
}